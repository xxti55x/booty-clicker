{
  "name": "@booty-clicker/game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:itch": "npm run build && node scripts/pack-itch.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:sim": "vitest run src/game/sim.test.ts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "vite": "^6.0.7",
    "vitest": "^3.0.5"
  }
}
